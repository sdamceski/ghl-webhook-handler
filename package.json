{
  "name": "starauto-ghl-webhook-listener",
  "version": "0.1.0",
  "private": true,
  "description": "GHL webhook Lambda (signature check + Redis allowlist + enqueue)",
  "license": "UNLICENSED",
  "main": "dist/handler.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "lint": "echo 'No lint configured'"
  },
  "dependencies": {
    "bullmq": "^5.16.0",
    "ioredis": "^5.6.2"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.152",
    "@types/node": "^20.11.30",
    "typescript": "^5.9.3"
  }
}
